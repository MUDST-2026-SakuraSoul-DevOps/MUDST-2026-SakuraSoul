# แนวทางทดสอบตาม User Story (SSK)

## E2E Testing

การทดสอบ E2E จำลองพฤติกรรมของผู้ใช้ผ่าน UI และครอบคลุมเส้นทางสำคัญตั้งแต่หน้าเว็บไปยัง API และฐานข้อมูลเมื่อเป็นชุด `live-api` รายละเอียดของแต่ละเคสใช้รูปแบบ `ID`, `Title`, `Preconditions`, `Steps`, `Input Data` และ `Expected Result` ตาม [E2E Test Case Catalogue](./e2e-test-cases.md)

| E2E project | API destination | Scope | Execution |
| --- | --- | --- | --- |
| `mock-api` | `src/api/mockApi.ts` | ตรวจ UI flow และผลลัพธ์บนหน้าเว็บโดยไม่แตะ Backend | CI ทุก PR |
| `stubbed-api` | Playwright `page.route` | ตรวจการตอบกลับของ API ในกรณีที่ต้องควบคุม response | CI ทุก PR |
| `live-api` | Spring Boot + PostgreSQL จริง | ตรวจ UI → API → Database และพฤติกรรมที่ mock อาจมองไม่เห็น | รันด้วยมือจนกว่าจะเตรียม CI environment |

ปัจจุบันมี E2E Test Case ทั้งหมด 26 เคส แบ่งเป็น 16 เคสที่ใช้ Mock/Stubbed และ 10 เคสที่ใช้ Live API จึงต้องรายงานผลแยกกัน ไม่รวมเป็นตัวเลขเดียวที่อาจทำให้เข้าใจว่าทุกเคสแตะ Backend จริง

การรายงาน E2E ต้องระบุ project ที่รัน, จำนวน passed/failed, ระยะเวลา, browser และ environment หากล้มเหลวให้เก็บ screenshot, trace และ log จาก `frontend/test-results/` เพื่อใช้วิเคราะห์ปัญหา

การตรวจด้วยมือใช้สำหรับ manual smoke demo ของเส้นทางสำคัญ เช่น Login → เพิ่มผู้เช่า → สร้างสัญญา → ตรวจ Dashboard → ออกบิล → Mark as Paid → Download PDF → Logout ไม่ใช่การทดแทน Automated E2E ทุกเคส

เอกสารนี้เป็นข้อตกลงร่วมกันว่า **ต้องพิสูจน์พฤติกรรมใด** ไม่ใช่รายการไฟล์เทสต์หรือรายงานว่าเทสต์ใดทำเสร็จแล้ว แหล่งที่มาของพฤติกรรมคือ [User Story ใน Jira](https://wishza2549.atlassian.net/issues/?jql=project%20%3D%20SSK%20AND%20issuetype%20%3D%20Story) และข้อตกลง API ใน `docs/` หากข้อกำหนดเปลี่ยน ให้แก้ User Story/ข้อตกลงที่เกี่ยวข้องก่อน แล้วจึงปรับเทสต์ตามพฤติกรรมที่ตกลงกัน

## วิธีใช้แผนนี้

1. เลือก scenario จากรหัส US/SSK ด้านล่าง แล้วเขียนเคสแบบ **Given–When–Then**: มีข้อมูล/สถานะอะไร → ผู้ใช้หรือระบบทำอะไร → ผลลัพธ์ที่สังเกตได้คืออะไร ภายในเทสต์สามารถจัดเป็น Arrange–Act–Assert ได้เหมือนกัน
2. ทดสอบในระดับที่พิสูจน์พฤติกรรมนั้นได้จริง ไม่จำเป็นต้องทำซ้ำทุกระดับ: กฎคำนวณใช้ unit test; การกรอก/คลิก/ข้อความบนจอใช้ frontend interaction test; การบันทึก ข้อผิดพลาด และข้อจำกัดข้อมูลใช้ backend/API integration test; เส้นทางสำคัญข้ามระบบใช้ E2E
3. ถ้าจำลอง API ใน frontend ให้จำลองเฉพาะขอบเขตการเชื่อมต่อ แล้วตรวจผลที่ผู้ใช้เห็นหรือคำขอที่ส่งจริง อย่าจำลองฟังก์ชันที่กำลังทดสอบจนเทสต์ผ่านทั้งที่พฤติกรรมจริงผิด
4. สำหรับข้อมูลผิด ให้ตรวจทั้งการปฏิเสธ/ข้อความแจ้งเตือนและการที่ข้อมูลเดิม **ไม่เปลี่ยน**; สำหรับข้อมูลถูก ให้ตรวจผลหลังบันทึกหรือโหลดใหม่ ไม่ดูเพียงว่าปุ่มถูกกด
5. กรณีที่ต้องอาศัยฐานข้อมูลจริง ความพร้อมกันของคำขอ หรือการสร้าง PDF ให้ทดสอบในระดับ backend/integration ด้วย; frontend interaction test อย่างเดียวพิสูจน์เรื่องเหล่านี้ไม่ได้

## พฤติกรรมที่ต้องพิสูจน์

| User Story | สิ่งที่ต้องทดสอบ | ระดับที่เหมาะ |
| --- | --- | --- |
| [US-01 / SSK-7](https://wishza2549.atlassian.net/browse/SSK-7) เข้าสู่ระบบ | S1 ข้อมูลถูกต้องแล้วเข้าถึง Dashboard; S2 ข้อมูลผิดแล้วเห็นข้อความและยังไม่เข้าระบบ; S3 session หมดอายุแล้วการทำรายการถัดไปพากลับหน้า Login | หน้าจอ + API; เส้นทางจริงใน E2E |
| [US-02 / SSK-8](https://wishza2549.atlassian.net/browse/SSK-8) ออกจากระบบ | S1 ออกจากระบบแล้วกลับหน้า Login และใช้ session เดิมเข้าหน้าป้องกันไม่ได้ | หน้าจอ + API |
| [US-03 / SSK-9](https://wishza2549.atlassian.net/browse/SSK-9) เพิ่มผู้เช่า | S1 กรอกข้อมูลที่จำเป็นแล้วบันทึกและเห็นในรายชื่อ; S2 ข้อมูลไม่ครบแล้วมีข้อความระบุช่องที่ผิดและไม่สร้างผู้เช่า | หน้าจอ + API |
| [US-04 / SSK-10](https://wishza2549.atlassian.net/browse/SSK-10) สร้างสัญญา | S1 เลือกผู้เช่า ห้องว่าง วันที่ และข้อมูลสัญญาที่จำเป็น แล้วสร้างสำเร็จ; ห้องแสดงสถานะมีผู้เช่า | หน้าจอ + backend integration; เส้นทางจริงใน E2E |
| [US-05 / SSK-11](https://wishza2549.atlassian.net/browse/SSK-11) กันสัญญาซ้อน | S1 วันที่ทับสัญญาห้องเดียวกันแล้วสร้างไม่ได้และเห็นเหตุผล; S2 คำขอสร้างที่ชนกันพร้อมกันต้องสำเร็จหนึ่งคำขอ อีกคำขอถูกปฏิเสธ | กฎช่วงเวลา + หน้าจอ; S2 ใช้ backend integration/ฐานข้อมูลจริง |
| [US-06 / SSK-12](https://wishza2549.atlassian.net/browse/SSK-12) แก้/สิ้นสุดสัญญา | S1 สิ้นสุดสัญญาแล้วสถานะสัญญาและห้องเปลี่ยนตามข้อกำหนด; S2 แก้วันที่จนทับสัญญาอื่นแล้วต้องปฏิเสธและคงข้อมูลเดิม | หน้าจอ + backend integration |
| [US-07 / SSK-13](https://wishza2549.atlassian.net/browse/SSK-13) ค้นหาผู้เช่า | S1 พิมพ์ชื่อแล้วผลลัพธ์เปลี่ยนทันที; S2 กรองตามสถานะสัญญาแล้วเหลือเฉพาะรายการที่ตรงเงื่อนไข | frontend interaction |
| [US-08 / SSK-14](https://wishza2549.atlassian.net/browse/SSK-14) ภาพรวมห้อง | S1 โหลดห้องตามขอบเขตอาคารครบ แสดงสถานะว่าง/มีผู้เช่า/ซ่อมบำรุงตรงกับข้อมูลที่ได้รับ และข้อมูลเปลี่ยนแล้วหน้าแสดงผลใหม่ถูกต้อง | frontend interaction + API; เส้นทางจริงใน E2E |
| [US-09 / SSK-15](https://wishza2549.atlassian.net/browse/SSK-15) คลิกห้อง | S1 ห้องว่างเปิดการทำรายการเช็กอิน; S2 ห้องมีผู้เช่าแสดงข้อมูลผู้เช่า/สัญญาของห้องนั้น; S3 ห้องซ่อมบำรุงแสดงข้อมูลงานซ่อมของห้องนั้น | frontend interaction |
| [US-10 / SSK-16](https://wishza2549.atlassian.net/browse/SSK-16) ใบเสร็จ | S1 ออกใบเสร็จจากสัญญาและข้อมูลรอบบิล แล้ว PDF แสดงรายการและยอดที่คำนวณถูกต้อง; ตรวจเนื้อหาเอกสารจริง ไม่ตรวจเพียงว่ามีไฟล์ | กฎคำนวณ + backend integration/PDF + หน้าจอ |
| [US-11 / SSK-17](https://wishza2549.atlassian.net/browse/SSK-17) เอกสารสัญญา | S1 สร้าง PDF จากสัญญาแล้วข้อมูลผู้เช่า ห้อง วันที่ และค่าเช่าตรงกับสัญญาต้นทาง | backend integration/PDF + หน้าจอ |
| [US-12 / SSK-18](https://wishza2549.atlassian.net/browse/SSK-18) บันทึกงานซ่อม | S1 บันทึกข้อมูลห้อง ประเภท/รายละเอียดงาน วันที่ และค่าใช้จ่ายตามที่กรอก แล้วรายการปรากฏในประวัติของห้องที่ถูกต้อง | หน้าจอ + API |
| [US-13 / SSK-19](https://wishza2549.atlassian.net/browse/SSK-19) ประวัติซ่อมรายห้อง | S1 เปิดประวัติของห้องแล้วเห็นเฉพาะงานของห้องนั้น เรียงตามวันที่ตามข้อตกลง | หน้าจอ + API |
| [US-14 / SSK-20](https://wishza2549.atlassian.net/browse/SSK-20) แจ้งเตือนงานซ่อมตามรอบ | S1 บันทึกความถี่และวันเริ่มต้นแล้วได้รอบแจ้งเตือน; S2 เมื่อถึงกำหนดและงานตามเวลาเริ่มทำงาน แอดมินเห็นการแจ้งเตือนในระบบ | หน้าจอ + backend integration โดยควบคุมเวลาในเทสต์ |
| [US-15 / SSK-21](https://wishza2549.atlassian.net/browse/SSK-21) สถานะซ่อมบำรุง | S1 ตั้งห้องเป็นซ่อมบำรุงแล้วไม่เปิดให้ทำสัญญาใหม่; S2 ปลดสถานะแล้วห้องที่ไม่มีสัญญามีผลเป็น `AVAILABLE` ส่วนห้องที่ยังมีสัญญามีผลเป็น `OCCUPIED` ตามโค้ดปัจจุบัน | หน้าจอ + backend integration |
| [US-16 / SSK-22](https://wishza2549.atlassian.net/browse/SSK-22) อัตราค่าบริการ | S1 บันทึกอัตราแล้วรอบบิลใหม่ใช้ค่าใหม่; S2 ค่าติดลบ/ข้อมูลไม่ถูกต้องถูกปฏิเสธและไม่เปลี่ยนค่าที่บันทึก; S3 ใบเสร็จเก่ายังคงอัตราเดิมเมื่อเปลี่ยน config | กฎ validation + หน้าจอ + backend/billing integration |
| [US-17 / SSK-23](https://wishza2549.atlassian.net/browse/SSK-23) คลังอุปกรณ์ | S1 เพิ่มชนิดอุปกรณ์; S2 เติมของแล้วจำนวนและประวัติการเติมเปลี่ยนถูกต้อง; S3 แสดงเตือนเมื่อของต่ำกว่าเกณฑ์ที่ตกลง; S4 ค้นหาตามชื่อ; S5 จำนวนเติมติดลบหรือไม่ใช่ตัวเลขต้องไม่บันทึก | หน้าจอ + backend integration |
| [US-18 / SSK-24](https://wishza2549.atlassian.net/browse/SSK-24) ส่งออกประวัติซ่อม | S1 มีข้อมูลแล้วดาวน์โหลดไฟล์ที่เปิดอ่านได้; S2 กรองก่อนส่งออกแล้วไฟล์มีเฉพาะรายการที่ตรงตัวกรอง; S3 ไม่มีข้อมูลแล้วแจ้งเตือนและไม่สร้างไฟล์ว่าง | กฎสร้างไฟล์ + frontend interaction |

## ความหมายของข้อมูลสำคัญในการเตรียมเคส

ตารางนี้อธิบายว่าแต่ละค่าหมายถึงอะไรและควรใช้พิสูจน์อะไร ส่วนตัวเลขในหัวข้อถัดไปเป็น **ข้อเสนอให้ทีมพิจารณา** ไม่ใช่กฎที่ระบบบังคับใช้อยู่แล้ว และตัวเลขในข้อมูลตัวอย่างของเทสต์ไม่ใช่กฎธุรกิจ

| ข้อมูล | ความหมาย | ประเด็นที่เทสต์ควรพิสูจน์ |
| --- | --- | --- |
| `roomNumber`, `roomId` | เลขห้องที่คนเห็น กับตัวระบุห้องในระบบ | รายการ สัญญา และงานซ่อมอ้างถึงห้องเดียวกัน; เลขห้องตัวอย่างไม่ใช่กฎตรวจรูปแบบเลขห้อง |
| `startDate`, `endDate` | วันเริ่มและวันสิ้นสุดสัญญา | ช่วงวันที่ถูกต้อง; การทับซ้อน/ไม่ทับซ้อน; วันสิ้นสุดที่ยังไม่กำหนด; ผลของการแก้ไขหรือสิ้นสุดสัญญา |
| `monthlyRent`, `billingCycle` | ค่าเช่าที่ใช้กับสัญญา และรอบบิลของสัญญา; สัญญาใหม่ใช้ค่าเช่าตามชนิดห้อง ไม่รับราคาที่ผู้ใช้ส่งมาแทน | ข้อมูลที่บันทึก/แสดงตรงกัน และใช้คิดบิลตามรอบที่เลือก |
| `electricRatePerUnit`, `waterRatePerUnit` | อัตราค่าไฟ/น้ำต่อหน่วยการใช้ | ข้อมูลที่ถูกต้องบันทึกได้; ค่าติดลบ/ไม่ใช่ตัวเลขถูกปฏิเสธ; จำนวนหน่วยที่ใช้คูณกับอัตราที่ถูกต้อง |
| `commonAreaFee`, `internetFee` | ค่าส่วนกลางและค่าอินเทอร์เน็ตต่อเดือนตามสัญญา API ปัจจุบัน | ค่าที่กรอกถูกนำไปคิดบิลตามช่วงเวลาที่ถูกต้อง; ค่าติดลบ/ไม่ใช่ตัวเลขถูกปฏิเสธ; ไม่เอาค่าตัวอย่างในเทสต์ไปใช้เป็นเพดาน |
| `electricUnits`, `waterUnits` | จำนวนหน่วยไฟ/น้ำที่ใช้ในรอบบิล | ผลรวมใบเสร็จเปลี่ยนตามจำนวนหน่วยและอัตราที่ใช้ในรอบนั้น |
| `cost` | ค่าใช้จ่ายของงานซ่อม | บันทึกและแสดงค่าใช้จ่ายของงานที่ถูกต้อง; ข้อมูลผิดไม่ถูกบันทึก |
| `stock`, `minStock`, `quantity` | จำนวนอุปกรณ์คงคลัง เกณฑ์ของใกล้หมดของอุปกรณ์แต่ละชนิด และจำนวนที่เติม/เบิก | เติมของแล้วจำนวนเปลี่ยนถูกต้อง; แจ้งเตือนตรงด้านของเกณฑ์; จำนวนเติมผิดรูปแบบหรือติดลบถูกปฏิเสธ |
| `status` | สถานะห้อง สัญญา หรืองานซ่อม โดยแต่ละอย่างมีชุดสถานะของตนเอง | ผลที่แสดงและการทำรายการต่อสอดคล้องกับสถานะจริง; ไม่ใช้สถานะของสิ่งหนึ่งแทนอีกสิ่งหนึ่ง |

### ค่าตั้งต้นที่เสนอให้ทีมยืนยันร่วมกัน

**สถานะโค้ดปัจจุบันก่อนปรับข้อกำหนด:** `electricRatePerUnit`/`waterRatePerUnit` รับ `0–1,000`; `commonAreaFee`/`internetFee` รับ `0–100,000` ทั้ง frontend และ backend และไม่รับค่าว่าง ค่าเช่าของสัญญาใหม่มาจากชนิดห้อง `SINGLE = 3,500`, `DOUBLE = 4,500` บาท ไม่ได้ใช้ `monthlyRent` ที่ส่งมาในคำขอ งานซ่อมรับ `cost = null` หรือ `0` ได้และไม่มีเพดานธุรกิจเฉพาะ (คอลัมน์ฐานข้อมูลเป็น `NUMERIC(10,2)`) ส่วน `stock`/`minStock` ต้องเป็นจำนวนเต็มไม่ติดลบ และการเติมของ `quantity` ต้องเป็นจำนวนเต็มมากกว่า `0` แต่ backend ยังไม่มีเพดานสต็อกต่อชนิด ฝั่ง frontend มี `maxStock` สำหรับตรวจสต็อก/จำนวนเติม ซึ่งยังไม่ใช่ฟิลด์ใน API จริง

ค่าด้านล่างเป็น **ข้อเสนอให้ทีมยืนยัน** ไม่ใช่ expected result ของเทสต์หรือกฎที่โค้ดบังคับใช้ทั้งหมด การปฏิเสธค่าติดลบเป็นกฎที่มีอยู่แล้ว แต่กฎอัตราน้ำไฟ `> 0`, เพดานค่ารายเดือนที่ต่ำลง และการจำกัดทศนิยมบางข้อยังต่างจากระบบปัจจุบัน โดยเฉพาะค่ารายเดือน: ถ้ามีการเรียกเก็บในเดือนนั้น ค่าเช่า/ค่าส่วนกลาง/อินเทอร์เน็ตไม่ควรเป็น `0` หรือว่าง; หากไม่เรียกเก็บจริงควรมีสถานะหรือกติกาแยก ไม่ใช้ `0` แทนโดยไม่มีคำอธิบาย จำนวนทศนิยมในตารางหมายถึงค่าที่ต้องการ **เก็บ/แสดง**; backend ปัจจุบันรับฟิลด์ที่เป็นทศนิยมเกิน 2 ตำแหน่งแล้วปัด ไม่ได้ปฏิเสธตั้งแต่รับข้อมูล

สัญลักษณ์เงินในระบบยังไม่ตรงกัน: frontend บน branch นี้ใช้ `¥` แต่ PDF ฝั่ง backend ใช้ `฿`; ตารางนี้ใช้ **บาทเป็นเป้าหมาย** หลังรวมงานเปลี่ยนสกุลเงินของ frontend จึงไม่ควรเขียนเทสต์ UI ว่าต้องเป็น `฿` จนกว่าจะรวมงานนั้น

| ชื่อตัวแปร | ชื่อภาษาไทย / เก็บค่าอะไร | สกุลเงินหรือหน่วยที่ตกลง | ค่าต่ำสุด | ค่าสูงสุด | ค่า `0` ได้ไหม | เว้นว่างได้ไหม | ทศนิยมได้กี่ตำแหน่ง |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `electricRatePerUnit` | อัตราค่าไฟต่อหน่วยที่ใช้คิดบิล ไม่ใช่ยอดค่าไฟทั้งเดือน | บาท/หน่วยไฟ | มากกว่า `0` (ก) | ไม่มีเพดานตัวเลขตายตัว; ตรวจตามบิล/วิธีคิดจริง (ก) | ไม่ได้ | ไม่ได้ | 2 |
| `waterRatePerUnit` | อัตราค่าน้ำต่อหน่วยที่ใช้คิดบิล ไม่ใช่ยอดค่าน้ำทั้งเดือน | บาท/หน่วยน้ำ | มากกว่า `0` (ก) | ไม่มีเพดานตัวเลขตายตัว; ตรวจตามบิล/วิธีคิดจริง (ก) | ไม่ได้ | ไม่ได้ | 2 |
| `commonAreaFee` | ค่าส่วนกลางที่เรียกเก็บทุกเดือน | บาท/เดือน | `1.00` | `10,000.00` | ไม่ได้ | ไม่ได้ | 2 |
| `internetFee` | ค่าอินเทอร์เน็ตที่เรียกเก็บทุกเดือน | บาท/เดือน | `1.00` | `5,000.00` | ไม่ได้ | ไม่ได้ | 2 |
| `monthlyRent` | ค่าเช่าต่อเดือนจากชนิดห้อง | บาท/เดือน | `2,000.00` | `20,000.00` | ไม่ได้ | ไม่ได้ | 2 |
| `cost` (งานซ่อม) | ค่าใช้จ่ายจริงของงานซ่อมหนึ่งรายการ; ยังไม่ทราบค่าใช้จ่ายให้เว้นว่าง | บาท/งาน | `0.00` | `15,000.00` | ได้ | ได้ | 2 |
| `electricUnits` | หน่วยไฟที่ห้องใช้จริงในรอบบิล ไม่ใช่อัตราต่อหน่วย | หน่วยไฟ/รอบบิล | `0.00` | `10,000.00` | ได้ | ไม่ได้ | 2 |
| `waterUnits` | หน่วยน้ำที่ห้องใช้จริงในรอบบิล ไม่ใช่อัตราต่อหน่วย | หน่วยน้ำ/รอบบิล | `0.00` | `1,000.00` | ได้ | ไม่ได้ | 2 |
| `stock` | จำนวนคงเหลือในคลังส่วนกลางของอุปกรณ์ชนิดนั้น ไม่ใช่ต่อห้อง | ชิ้น/ชนิด | `0` | `maxStock` ของชนิดนั้น (ข) | ได้ | ไม่ได้ | 0 |
| `minStock` | เกณฑ์เตือนของใกล้หมด ตั้งแยกต่อชนิด ไม่ใช่จำนวนขั้นต่ำที่ต้องมีจริง | ชิ้น/ชนิด | `0` | `maxStock` ของชนิดนั้น (ข) | ได้ (ไม่แจ้งเตือน Low Stock) | ไม่ได้ | 0 |
| `maxStock` | เพดานจำนวนในคลังของอุปกรณ์ชนิดนั้น ตั้งแยกต่อชนิดใน frontend | ชิ้น/ชนิด | ไม่น้อยกว่า `stock` และ `minStock` | ไม่กำหนดเพดานกลาง (ข) | ได้เฉพาะเมื่อ `stock` และ `minStock` เป็น `0` | ไม่ได้ | 0 |
| `quantity` (เติมของ) | จำนวนที่เติมเข้าคลังส่วนกลางของอุปกรณ์ชนิดนั้นในครั้งนั้น | ชิ้น/ครั้ง | `1` | `maxStock - stock` ของชนิดนั้น (ข) | ไม่ได้ | ไม่ได้ | 0 |

(ก) **กฎที่เสนอให้บังคับ:** อัตราต้องเป็นตัวเลขมากกว่า `0`; ไม่กำหนดขั้นต่ำ/ขั้นสูงเป็นตัวเลขคงที่ เพราะต้องตรวจอัตราที่เรียกเก็บและวิธีคำนวณจากบิลจริง **ช่วงที่เสนอให้เตือนเพื่อตรวจซ้ำ ไม่ใช่ปฏิเสธอัตโนมัติ:** ค่าไฟต่ำกว่า `2` หรือสูงกว่า `6` บาท/หน่วย; ค่าน้ำต่ำกว่า `8` หรือสูงกว่า `20` บาท/หน่วย ค่าเริ่มต้นในโค้ด `8` และ `18` เป็นข้อมูลตัวอย่าง ไม่ใช่หลักฐานว่าเรียกเก็บได้ตามกฎ [สคบ. ระบุว่าค่าน้ำและค่าไฟของธุรกิจให้เช่าที่อยู่อาศัยต้องไม่เกินอัตราที่ผู้ให้บริการเรียกเก็บจากผู้ประกอบธุรกิจ](https://www.ocpb.go.th/news_view.php?nid=18149) ระบบปัจจุบันคิด `หน่วยที่ใช้ × อัตราคงที่` ยังไม่ใช่[อัตราก้าวหน้าตามตัวอย่างของ กฟภ.](https://www.pea.co.th/news/corporate-news/2412) จึงต้องตกลงวิธีคำนวณก่อนเขียน expected result ของบิลแบบขั้นบันได ทั้งการตรวจเทียบบิลและการเตือนตามช่วงยังไม่มีในโค้ดปัจจุบัน

(ข) สต็อกเป็นของ **คลังส่วนกลางทั้งตึก แยกตามชนิดอุปกรณ์** ไม่ใช่จำนวนต่อห้องหรือผลรวมทุกชนิด `stock = 0` หมายถึงของหมดจริง ส่วน `minStock = 0` ทำให้เงื่อนไข `stock < minStock` ไม่เป็นจริง จึงไม่ขึ้น `LOW_STOCK` แม้ของหมด; หากต้องการเตือน ควรตั้ง `minStock > 0` ตัวอย่าง frontend ปัจจุบันตั้ง `maxStock` แยกเป็นหลอดไฟ `200`, ไส้กรองแอร์ `60`, ข้อต่อท่อ `120` ชิ้น ตัวเลขเหล่านี้เป็นเพียงข้อมูลตัวอย่าง ไม่ใช่เพดานทุกชนิด Frontend ตรวจ `stock`, `minStock`, `quantity` เทียบกับ `maxStock` ของชนิดนั้น แต่ backend ยังไม่มีฟิลด์/กฎ `maxStock` จึงยังไม่ใช่พฤติกรรมข้ามระบบ

หากอยากรับค่าผิดปกติแต่ยังอยู่ในเพดาน ให้เตือนเพื่อทวนก่อนบันทึกแทนการปฏิเสธทันที: ค่าส่วนกลาง `> 1,500` บาท/เดือน, อินเทอร์เน็ต `> 1,500` บาท/เดือน, ค่าเช่า `> 15,000` บาท/เดือน (กรณีเพิ่มชนิดห้องในอนาคต), ค่าซ่อม `> 100,000` บาท/งาน, ไฟ `> 1,000` หน่วย/ห้อง/รอบบิล หรือน้ำ `> 100` หน่วย/ห้อง/รอบบิล ตัวเลขเตือนเหล่านี้ก็เป็นข้อเสนอ ไม่ใช่กฎของโค้ดปัจจุบัน

| หัวข้อ/ตัวแปร | ความหมายภาษาไทย | ค่าจากโค้ดปัจจุบัน / ข้อเสนอ |
| --- | --- | --- |
| การปัดเศษเงิน (ข้อตกลงร่วม ไม่ใช่ฟิลด์ API) | วิธีปัดเศษเมื่อคำนวณยอดเงินและส่งค่าระหว่าง frontend/backend | backend ปัด `HALF_UP` เหลือ 2 ตำแหน่ง; เสนอให้ frontend แสดงผลตรงกันหลังเปลี่ยนเป็นบาท |
| `stock` เทียบกับ `minStock` | เกณฑ์ Low Stock | ใช้กฎเดิม: `stock < minStock` คือ `LOW_STOCK`; เท่ากันคือ `IN_STOCK` |
| `room.status` ก่อนซ่อมเป็น `AVAILABLE` | หลังปลดสถานะ `MAINTENANCE` โดยไม่มีสัญญาที่มีผลวันนี้ | `AVAILABLE` ตามโค้ดปัจจุบัน |
| `room.status` ก่อนซ่อมเป็น `OCCUPIED` | หลังปลดสถานะ `MAINTENANCE` ขณะสัญญายังมีผล | `OCCUPIED` ตามโค้ดปัจจุบัน |
| ฟอร์แมตไฟล์ส่งออก (ยังไม่มีฟิลด์เลือกฟอร์แมตใน API) | ประเภทไฟล์ที่ต้องรองรับใน US-18 | ปัจจุบัน `CSV` เท่านั้น; ฟอร์แมตอื่นต้องตกลงเพิ่ม |
| ตัวเลือกฟอร์แมตบนหน้าจอ | ต้องให้ผู้ใช้เลือกประเภทไฟล์ก่อนส่งออกหรือไม่ | ปัจจุบันไม่ต้องเลือกเพราะมีเพียง `CSV`; หาก User Story ต้องการหลายฟอร์แมตให้ตกลงเพิ่ม |

เมื่อทีมยืนยันข้อใดแล้ว ให้บันทึกค่าหรือกฎไว้ใน User Story/ข้อตกลง API ที่เป็นต้นทางก่อน จากนั้นจึงเพิ่มเคสขอบเขต เช่น ค่าต่ำสุด/สูงสุด ค่าพอดีขอบ ค่าที่เลยขอบ ค่าว่าง และค่าที่ต้องเตือนในเทสต์ที่เกี่ยวข้อง หากข้อเสนอต่างจากพฤติกรรมระบบปัจจุบัน ให้ทีมยืนยันการเปลี่ยน requirement ก่อนแก้โค้ดหรือเทสต์

## แหล่งอ้างอิงอื่น

- [ข้อตกลง API สัญญาเช่า](../api-contract-lease.md)
- [ข้อตกลง API ใบเสร็จ](../api-contract-billing.md)
- [ข้อตกลง API งานซ่อม](../api-contract-maintenance.md)
- [งานทดสอบข้ามระบบ SSK-26](https://wishza2549.atlassian.net/browse/SSK-26) ใช้ดูขอบเขต integration/E2E; สถานะงานจริงให้ตรวจจากงานที่ทำ ไม่ใช้เอกสารนี้เป็นรายการความคืบหน้า
