# เปิด backend จำลองในเบราว์เซอร์ (src/api/mockApi.ts)
#
# ตั้ง 1 ระหว่างที่ endpoint สัญญาเช่าฝั่ง Spring ยังไม่มี หน้าจอทั้งหมดจะรันได้
# โดยไม่ต้องเปิด backend เลย ตั้ง 0 เมื่ออยากต่อ backend จริงที่ localhost:8080
# (ตอนนี้ต่อจริงได้ห้อง ผู้เช่า สัญญาเช่า และอัตราค่าสาธารณูปโภค
#  ส่วน PUT/terminate ของสัญญายังไม่มี จะขึ้น 404 จนกว่า SSK-12 จะเสร็จ)
VITE_API_MOCK=1
